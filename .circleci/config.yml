version: 2.1

executors:
  linux-22:
    docker:
      - image: cimg/node:22.13

commands:
  install-deps:
    steps:
      - restore_cache:
          keys:
            - npm-deps-{{ checksum "package-lock.json" }}
            - npm-deps-
      - run:
          name: Install dependencies
          command: npm ci
      - save_cache:
          key: npm-deps-{{ checksum "package-lock.json" }}
          paths:
            - ~/.npm

jobs:

  # ── Desktop: lint + typecheck (all branches) ─────────────────────────────
  desktop-lint-typecheck:
    executor: linux-22
    steps:
      - checkout
      - install-deps
      - run:
          name: Lint
          command: npx turbo run lint --filter=@sekel/desktop
      - run:
          name: Type check
          command: npx turbo run typecheck --filter=@sekel/desktop

  # ── Desktop: unit tests (all branches) ───────────────────────────────────
  desktop-unit-tests:
    executor: linux-22
    steps:
      - checkout
      - install-deps
      - run:
          name: Rebuild better-sqlite3
          command: npm rebuild better-sqlite3 --build-from-source
      - run:
          name: Install Rollup native binary
          command: npm install --no-save rollup
          working_directory: apps/desktop
      - run:
          name: Unit tests (desktop)
          command: npx turbo run test:unit --filter=@sekel/desktop
      - run:
          name: Component tests
          command: npx turbo run test --filter=@sekel/components

# Platform builds + publishing live in .github/workflows/release.yml
# (triggered by `desktop/v*.*.*` tag push — builds matrix on
# windows-latest / macos-latest / ubuntu-latest, signs Windows installers
# via Azure Artifact Signing, and publishes to GitHub Releases + Cloudflare R2).
workflows:
  ci:
    jobs:
      - desktop-lint-typecheck
      - desktop-unit-tests:
          requires:
            - desktop-lint-typecheck
